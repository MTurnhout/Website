import { JsonDateHttpInterceptor } from "./json-date-http-interceptor";

describe("JsonDateHttpInterceptor", () => {
  it("should create an instance", () => {
    expect(new JsonDateHttpInterceptor()).toBeTruthy();
  });
});
